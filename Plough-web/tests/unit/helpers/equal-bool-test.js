
import { equalBool } from 'pmo-web/helpers/equal-bool';
import { module, test } from 'qunit';

module('Unit | Helper | equal bool');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = equalBool([42]);
  assert.ok(result);
});

