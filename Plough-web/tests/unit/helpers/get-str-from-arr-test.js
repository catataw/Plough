
import { getStrFromArr } from 'pmo-web/helpers/get-str-from-arr';
import { module, test } from 'qunit';

module('Unit | Helper | get str from arr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getStrFromArr([42]);
  assert.ok(result);
});

