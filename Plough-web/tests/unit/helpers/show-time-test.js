
import { showTime } from 'pmo-web/helpers/show-time';
import { module, test } from 'qunit';

module('Unit | Helper | show time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showTime([42]);
  assert.ok(result);
});

