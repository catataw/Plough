
import { addStyle } from 'pmo-web/helpers/add-style';
import { module, test } from 'qunit';

module('Unit | Helper | add style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addStyle([42]);
  assert.ok(result);
});

