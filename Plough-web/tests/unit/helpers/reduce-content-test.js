
import { reduceContent } from 'pmo-web/helpers/reduce-content';
import { module, test } from 'qunit';

module('Unit | Helper | reduce content');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reduceContent([42]);
  assert.ok(result);
});

