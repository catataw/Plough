
import { realIndex } from 'pmo-web/helpers/real-index';
import { module, test } from 'qunit';

module('Unit | Helper | real index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = realIndex([42]);
  assert.ok(result);
});

